<h1>Editing Url</h1>

<%= form_for(@url) do |f| %>
  <% if @url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@url.errors.count, "error") %> prohibited this url from being saved:</h2>

      <ul>
      <% @url.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <span class="field">
    <%= f.text_field :long_url, placeholder: 'Long URL' %>
  </span>
  <span class="actions">
    <%= f.submit 'Shorten URL', style: "margin: 2px 3px 0px 10px;" %>
  </span>
<% end %>

<br/>
<br/>
<%= link_to 'Show', @url %> |
<%= link_to 'Back', new_url_path %>
